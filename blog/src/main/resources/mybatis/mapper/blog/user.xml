<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="gdut.ff.mapper.UserMapper">
    
    <resultMap id="userMap" type="gdut.ff.domain.User">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="birthday" property="birthday" />
        <result column="login_name" property="loginName"/>
        <result column="pass_word" property="passWord"/>
        <result column="img" property="img"/>
        <result column="signature" property="signature"/>
    </resultMap>
    
    <select id="findAll" resultMap="userMap">
        SELECT *
        FROM user
    </select>
    
    <select id="findOneById" resultMap="userMap">
        SELECT * 
        FROM user 
        WHERE id = #{id}
    </select>
    
    <insert id="saveUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(name,birthday,login_name,pass_word,signature,img)
        VALUES(#{name},#{birthday},#{loginName},#{passWord},#{signature},#{img})
    </insert>
    
    <update id="updateUser">
        UPDATE user 
        SET name = #{name},
            birthday = #{birthday},
            login_name = #{loginName},
            pass_word = #{passWord},
            signature = #{signature},
            img = #{img}
         WHERE id = #{id}
    </update>
    
    
</mapper>